---
title: Taking shape...
date: "2026-05-09"
---

I feel like I should start pouring gas on the backend part now. Like try to take control over the frontend from the backend. Make it all stateful and controllable through API requests to backend for agent server with MCP tool and authorization.

When the read tool reads a file it enters a cache of filename: content
when write outputs a new file, the previous content, if any, should be the before_content for monaco diff. after it writes it writes the file to the filesystem and updates document model of ide so it is loaded in editor also
same with edit tool

---
title: Scared
date: "2026-05-23"
---

I need a job.

Feel like I have really lofty ambitions and zero bonafides to make people take note. The code I am making is completely vibe coded probably not that great. At least crow-ui anyway. crow-cli is pretty legit.

I don't know. Who cares?

I mean I guess I do. But I don't know this UI thing has been a big learning experience for me. IDEs are actually pretty damn complicated. And I have a lot more features to add too!

Oh my god so many features. But sitting down and listing them all off doesn't really seem to be that that useful so I've just sort of been liking picking the thing that bugs me and knocking it out. Doing less playwright testing since I'm using the server for the tasks and the electron app for testing. have my development server finally.

So I turned off zed today. Removed it. I hadn't been using. I'd been testing crow-ui myself by using crow-ui to test. I found a pretty stable version and just talked to it running on 3928 and I did all the building and testing of the electron app and it works really really well. Like I'm sort of shocked at how well this works to be honest. I guess you can just sort of glue things together in HTML eh?

Anyway this is really cool. I'm probably going to use this as like my generic UI for the forseeable future. 


Okay so the explorer gets out of sync with the actual filesystem. that's not good. we'll need to work on that.

I keep coming back to wanting to have a jupyter notebook extension like setup.

Some way to package crow-cli more closely with crow-ui. I guess this means it is time for deeper integration and crow-cli work. And I'm basically laying it out now. hold on.

---
I feel like we should be able to pass in a config that's very very very similar to what you see in crow-cli/crow-cli's configure.py as an argument to our crow-cli agent. specifically I want to be able to overwrite any of the default configs loaded from config_dir with a config.yaml file that looks a LOT like ~/.crow/config.yaml right? but can also include a system prompt string. or better yet. let's make the system prompt string point to a file location in the yaml that gets loaded.

I say this because I am ready to have a system where we have a system prompt editor view in crow-ui where we can use our existing ACP configuration framework in combination with a system prompt/mcp editor view to create customized crow-cli agents inside crow-ui

but to make this really effortless, we need better args to crow-cli acp! so the main work is 

adding a config.yaml file that will overwrite what we load in from the config dir, which is NOT changed. we are not going to add new providers. just

db_uri
mcp_servers
max_retries_per_step
MAX_COMPACT_TOKENS
MAX_TOKENS
chunk_log
system_prompt

as appropriate and they will override the others. can we do that please?

but first get comfortable. understand the flow. right? we need to make sure our CLI argument override these.

I do something like this in crow-cli/sandbox/repl-agent/main.py where I override the settings from the defaults. we want to use a config.yaml to be able to override from command line so we can construct custom agents in crow-ui via the already existing acp client configuration panel. we just add in a system prompt editor pane with some configuration sliders/forms and MCP config setup for what we want to pass in to the agent.

---
oh yeah and besides being out of sync the file explorer is not showing hidden files, which is not what we want.

---

I'm realizing there's quite a bit I need to do with crow-cli to accomodate crow-ui. Especially the backend mesh stuff. I think I should go ahead and create some kind of conversation_history tool for accelerating the discovery of previous agent information.

I also need to move the text and images into separate lancedb or into postgres and some alternative to minio for the image blobs and we can worry about embedding/encoding later, but we need to move the image blobs out of the messages json. properly split that out so it doesn't sink performance.

So I'm a bit nervous because this is my first real coupling between crow-ui and crow-cli. First crossover feature. And I think it's worth the complexity/complecting. 


1. ~~We're overzealous when we're trying to select text to add a [] around. we need to back off enough to let multi-line `Ctrl+[` do it's job in monaco. it works with single line, but when we have blobs of text selected our very aggressive "make sure you add parens or { or [ around things" might be a little over aggressive and not doing the right thing in this case.~~
2. Okay having some issues with lagging dirty file indicators. Sort of freaking me out lol.
3. and I can't type underscores into the file search in monaco
4. also we really do need to make sure it scrolls to the very bottom after a tool accordian opens. having it open to just the top is not great.
5. We can't copy and paste information from markdown into the rich text editor. Not showing up after paste.

- Get the /api/acp/session/<session-id>/relay in there
- create /summary slash command for getting compaction-like summary without creating new agent in crow-cli
- system prompt editor for crow-cli instances
- configuration editor as part of system prompt crow-cli agent editor
- some kind of spec-kit like flow but now with agent orchestration through acp mesh relay
- UI for adding api key and base url and models for crow-cli

---
title: pivot
date: "2026-05-28"
---

So I started playing around with typst because I am using a resume I don't have the source code for any longer. So me and the agent start looking into building whole websites with typst because even if it only generates a single page at a time from a document and that's a "limitation" like oh well? They're called **chapters**.

Not everything needs to be an infinite scroll. We used to have this thing called _pagination_ where instead of JIT-ing bits of an infinite firehose of information to a user, the information was indexed into **pages**.

And we would call those collections of pages a _website_ lol.

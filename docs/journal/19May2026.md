---
title: waiting
date: "2026-05-19"
---

I guess I need to put in a fresh round of applications again while I wait to hear back from final interview with most recent company I've gotten that far with. I'm nervous. Hoping this goes well. We'll see. Having a bad feeling I'm putting too many eggs in this basket and need to branch out. 

Just replied to a recruiter's email and feel better almost immediately. It's one of those ones where they're coming in from github going to my personal email instead of my work/looking for work email account. So yeah I need to take a chill pill and fix the things that are wrong with crow-cli and crow-ui. Starting to feel a bit like this thing is almost ready to ship. 

What I need to do to make it ready to ship:
- fix backend state/refactor
- better interop with crow-cli — you should be able to control / configure everything about crow-cli from inside crow-ui as it's the UI for crow-cli
- something marimo/jupyter like for viewing notebooks or python files either/or
- control/monitoring for docker compose that is running searxng
- you should not need to crow-cli init to set up provider for crow-cli if you install crow-ui
- .deb/.exe(?)/.dmg and packing for installing as service with crow-cli install web (are we supporting windows?)
- MCP servers for controlling the backend agents
- auth for backend endpoints
- git support for basic like dirtyFile but changes against .git
- some kind of sophisticated debugging view for crow-cli where you can see the ACP JSON-RPC and the configs or whatever. Needs to be a view on the side you can maximize
- use many many more of FlexLayout's built in functionality instead of fighting it/rebuilding vscode style stuff
- down a rabbit hole with terminal tool of course
